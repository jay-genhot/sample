package no.trinnvis.dabih;

import java.io.IOException;
import org.junit.Test;

public class BoxfusePayloadLargeTest {
    @Test
    public void payloadIsUp() throws IOException {
        /*
        String html = new JdkRequest(System.getProperty("boxfuse.instance.url", "http://localhost:8080"))
            .uri().path("/accounts/tgewdsg@mailinator.com").back()
            .method(Request.GET)
            .header(HttpHeaders.ACCEPT, MediaType.TEXT_HTML)
            .fetch()
            .as(RestResponse.class)
            .assertStatus(HttpURLConnection.HTTP_OK)
            .body();


        assertThat(html, containsString("xxx"));
        */
    }
}